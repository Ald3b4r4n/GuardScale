#############################################
# GuardScale — Exemplo de variáveis (.env)  #
# Copie este arquivo para .env e preencha.  #
# Nunca versione .env com segredos reais.   #
#############################################

# Banco de dados
MONGO_URI=mongodb://127.0.0.1:27017/guardscale

# Segurança
JWT_SECRET=defina_um_segredo_forte_em_producao

# Admin seed (apenas no primeiro start sem usuários)
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=admin123

# Ambiente e fuso
NODE_ENV=development
TZ=America/Sao_Paulo

# CORS permitido (separe por vírgula)
# CORS_ORIGIN=http://localhost:3000

# Suporte / e-mail (opcional)
SUPPORT_EMAIL_TO=suporte@dominio.com
SUPPORT_EMAIL_FROM="GuardScale <no-reply@dominio.com>"

# SMTP (opcional, para /api/auth/request)
# Preencha conforme seu provedor. Use senha de app quando aplicável.







# Observações
# - Em produção, defina `secure=true` nos cookies; a app faz isso automaticamente
#   quando `NODE_ENV=production` e usa HTTPS.
# - Não publique credenciais reais em issues, commits ou README.